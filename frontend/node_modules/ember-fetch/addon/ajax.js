import fetch from 'fetch';
export default function ajax(input, init) {
    return fetch(input, init).then(response => {
        if (response.ok) {
            return response.json();
        }
        throw response;
    });
}
